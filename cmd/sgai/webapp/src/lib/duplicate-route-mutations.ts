export const duplicateRouteWorkspaceControlsDisabledReason = "Some interactive controls are unavailable on duplicate-name workspace routes because the current API still targets workspaces by basename only.";

export const duplicateRouteActionButtonsDisabledReason = "Action buttons are unavailable on duplicate-name workspace routes because the current API still targets workspaces by basename only.";

export const duplicateRouteResponseSubmissionDisabledReason = "Response submission is unavailable on duplicate-name workspace routes because the current API still targets workspaces by basename only.";
